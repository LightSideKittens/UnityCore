сейчас всё ещё есть какая-то проблема с использованием шрифтов. я даже плохо понимаю что происходит. Но логика должна быть такая:

1. У нас есть текстовый компонент, на текстовом компоненте есть UniTextFontAsset (далее называем его "currentFontAsset").
2. При вводе текста, должен происходить поиск глифа в currentFontAsset. 
3. Если глиф найден в currentFontAsset, то пытаемся получить его из атласа currentFontAsset и отрисовать. 
4. Если его нет в атласе currentFontAsset, то сначала добавляем его в атлас ИМЕННО для currentFontAsset, а потом отрисовываем. 
5. Если char/codepoint (я не разбираюсь в том, что должно быть в шрифте) не найден в currentFontAsset, то идём по списку Fallback шрифтов у currentFontAsset. Каждый шрифт из списка используем в качестве "currentFontAsset" и прогоняем его через пункты 2-5
6. Если предыдущие шаги не дали результат, то идём в  UniTextSettings и используем список  FallbackFontAssets и прогоняем каждый шрифт через пункты 2-5.

Важно чтобы была защита от бесконечного цикла. Проверь текущую логику и приведи её в соответствие с этим планом